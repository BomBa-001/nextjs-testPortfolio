import NavBar from "./navigation/navBar";

const Header = () => {
  return <>
  <header className="fixed start-0 inset-x-0 w-full bg-red-500 bg-opacity-20">
    <NavBar/>
  </header>
  </>;
};
export default Header;
